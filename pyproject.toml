[project]
name = "flowbetter-irop"
version = "0.1.0"
description = "Synthetic airline recovery sandbox"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.30,<1", "httpx>=0.27,<1"]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
